<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../../partials/head %>
   
</head>

<body class="sidebar_main_open sidebar_main_swipe">
 	
     <!--Top Header-->
    <% include ../../partials/mainheader %>
 
    <!--Top Header-->
    <% include ../../partials/sidemenu %>
    
    <!--Start Content-->

<div id="page_content">
    <div id="page_content_inner">
		<h3 class="heading_b uk-margin-bottom">Staff</h3>
        <!--Geographical Select-->
        <form id="form" class="new" enctype="multipart/form-data" action="/staff/save" method="post"> 
        <div class="md-card">
            <div class="md-card-content">
              <div class="uk-grid">
                  <div class="uk-width-medium-1-2">
                      <div class="uk-form-row valid">
                        <label for="area">Title</label>
                        <input type="text" class="md-input md-input-success" id="staffname"  name="staffname" placeholder="Staff Name"/><span class="req">*</span>
                      </div>   
                      <div class="uk-form-row">
                        <label for="area">Description</label>
                        <textarea  class="md-input" id="description" name="description" placeholder="Description"></textarea>
                      </div>
                      <div class="uk-form-row valid">
                        <label for="area">Grop</label>
                        <select class="md-input" id="group" name="group">
                          <option value="0">School</option>
                          <option value="1">Class</option>
                          <option value="2">Student</option>
                          <option value="3">Finance</option>
                        </select>  
                        <span class="req">*</span>
                      </div>
                      <div class="uk-form-row">
                            <button type="submit" class="md-btn md-btn-success"><i class="uk-icon-save white paddingleft"></i>Save</button>
                           <a href="/staff" class="md-btn md-btn-success"><i class="uk-icon-remove white paddingleft"></i>Cancel</a>
                      </div>
                  </div>                    
              </div>
            </div>
        </div>    
    </form>
    <!-- End Geographical Select-->  
    </div>
</div>

<!--End Content--> 


<!--Start Script-->

 

    
     <script>

        $("#doj").datepicker();
        $("#dob").datepicker();     

        function readURL(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    $('#blah').attr('src', e.target.result);
                }
                reader.readAsDataURL(input.files[0]);
                console.log(input.files[0].name);
                document.getElementById("imagename").value = input.files[0].name ;
            }
        }
    
        $("#imgInp").change(function(){
            readURL(this);

        });
        
        $.ajax({ 
            url: "/staff/design",
            cache: false, 
            success: function(data){
                var options="";
                for( var i=0;i<data.custdata.length;i++) {
                   options += '<option value="' + data.custdata[i].id + '">' + data.custdata[i].design_name + '</option>';
                }
                $('#designid').html(options);
            }
            ,error: function(jqXHR, textStatus, err){
              alert('text status '+textStatus+', err '+err);
            }
        });
         
    </script>
   
    <script src="/moment/min/moment.min.js"></script>
    <script src="/js/uikit_custom.min.js"></script>
    <script src="/js/altair_admin_common.min.js"></script>
    <script src="/js/valid.js"></script>
    <script type="text/javascript" src="/js/datatables.js"></script>
    <script src="/js/export.js"></script>
   
    
</body>
</html>